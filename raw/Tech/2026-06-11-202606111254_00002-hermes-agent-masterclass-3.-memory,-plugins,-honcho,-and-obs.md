---
tags: [技術]
source: LINE
date: 2026-06-11
log_id: 202606111254_00002
url: https://youtu.be/ZKZLko9kLm4
platform: youtube
content_type: video
extraction_status: ok
needs_review: false
needs_local_worker: false
worker_status: done
worker_type: omnichannel_md
worker_retry_count: 0
summary: 1. 本模組詳解Hermes內建記憶系統，包含memory.md與user.md兩份Markdown文件，用以儲存代理對項目、環境及使用者特性的記憶，並於每次會話啟動時載入至系統提示中，確保會話連續性與上下文一致性。 2. 除內建記憶層外，還介紹三層擴展功能：FTS 5會話搜索可回溯歷史對話、外部插件提供語義回憶與身份建模、Obsidian技能則整合結構化知識庫，四層可疊加運作以強化代理的記憶與推理能力。
status: unread
worker_error: ""
worker_processed_at: 2026-06-11T13:37:02+08:00
worker_name: mac-mini-omnichannel
---
# Hermes Agent Masterclass: 3. Memory, Plugins, Honcho, and Obsidian

## 摘要
1. 本模組詳解Hermes內建記憶系統，包含memory.md與user.md兩份Markdown文件，用以儲存代理對項目、環境及使用者特性的記憶，並於每次會話啟動時載入至系統提示中，確保會話連續性與上下文一致性。
2. 除內建記憶層外，還介紹三層擴展功能：FTS 5會話搜索可回溯歷史對話、外部插件提供語義回憶與身份建模、Obsidian技能則整合結構化知識庫，四層可疊加運作以強化代理的記憶與推理能力。

## 重點
- 1. 本模組詳解Hermes內建記憶系統，包含memory.md與user.md兩份Markdown文件，用以儲存代理對項目、環境及使用者特性的記憶，並於每次會話啟動時載入至系統提示中，確保會話連續性與上下文一致性。
2. 除內建記憶層外，還介紹三層擴展功能：FTS 5會話搜索可回溯歷史對話、外部插件提供語義回憶與身份建模、Obsidian技能則整合結構化知識庫，四層可疊加運作以強化代理的記憶與推理能力。

## 原始內容
Welcome to Module 3 of my Hermes Agent Masterclass. This video is going to be on memory, very important topic. So Modules 1 and 2 got Hermes running, sandboxed, always on reachable from your phone, from telegram or from discord. What you might not realize is that Hermes' memory system has been quietly working the whole time. Today we open it up, explain exactly how the built-in memory works, demonstrate it live, and then look at three more layers you can use to extend it. So this is Module 3 of my Hermes Agent Masterclass. So here's how Hermes remembers. There's a built-in memory layer, you can see at this directory.hermyslashmemories, and the two main markdown files are memory.md and usermd. And these are the core parts of the built-in Hermes memory system. And these have been active since the first time we booted Hermes up and started talking to it. Hermes has been reading from them at the start of every session and writing to them as we've been using it. That's the built-in memory system. So let's take a quick look at an actual agent and the memory.md and user.md file. Before we get into that, we should note that the memory system is small. It's about 1,300 tokens of system palm budget across both files. It's bounded. Hard character limits force the agent to carry it rather than accumulate. Well designed, every constraint has a reason and we'll get into that in a few sides. And most people who use Hermes never actually understand what's going on inside it. So today we're going to fix that. So in the first half of this module, we're going to take apart this layer 1, the built-in markdown, cover how it loads, how the agent writes to it, the frozen snapshot pattern, the hidden safety features that make it work, and how to see it deliberately. Then the second half, we're going to look at three more layers that extend layer 1 of the memory. The FTS 5 session search lets the agent dig through any prior conversation, then you have external provider plugins, Hanjo, Memo, etc. They add semantic recall and identity modeling. And then there's the obsidian skill which turns the agent into a curator of a structured knowledge base. But each one solves a problem, layer 1 can't. But let's start there with the built-in memory system. And you could see it here down on layer 1, the memory empty and user empty files, then you see layer 2, the session search. And this is full text recall over every prior conversation. And this is built in the agent and it's always on. Layer 3, like a set external provider plugins, Hanjo Memo hindsight, there's eight options right now and there's always new ones coming up, the semantic recall plus identity modeling. Layer 4 is obsidian vault. So this one's different. It's not a plugin, it's a skill. The agent writes structured notes, you can curate in your actual obsidian app. And the nice thing is that these aren't really alternatives, they stack, each one is additive. By the end of this episode, all four are going to be running at the same time. But first let's get to layer 1 more, the built-in markdown files. So there's two files and like I said, both live at .hermys/memories. So the two files are memory md and this is the agent's notes about projects, environment, decisions, lessons, learns, 2,200 characters, about 800 tokens of system prompt budget. User md, this is the agent's model of who you are. Your role preferences communication style, about 375 characters, about 500 tokens. Let me take a look at these files in

...（內容已截斷）

## 內含連結
- https://youtu.be/ZKZLko9kLm4

## 媒體
- 未擷取

## 擷取狀態
- 平台：youtube
- 類型：video
- 擷取狀態：ok
- 需要人工確認：否

## 原文連結
https://youtu.be/ZKZLko9kLm4
